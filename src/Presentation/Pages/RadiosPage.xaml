<Page x:Class="Rok.Pages.RadiosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Rok.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:dto="using:Rok.Application.Dto"
      mc:Ignorable="d"
      x:Name="radiosView">

    <Grid RowSpacing="16" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="Play a URL" Click="OnPlayUrlClick" />
            <Button Content="Add to favourites" Click="OnAddClick" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Stations}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:RadioStationDto">
                    <Grid ColumnSpacing="12" Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind StreamUrl}" Opacity="0.6" FontSize="12" />
                        </StackPanel>

                        <Button Grid.Column="1" Content="Play"
                                Click="OnPlayClick"
                                Tag="{x:Bind}" />

                        <Button Grid.Column="2" Content="Delete"
                                Click="OnDeleteClick"
                                Tag="{x:Bind}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
