<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Rok.Pages.ArtistsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Rok.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:artists="using:Rok.ViewModels.Artist"
      xmlns:common="using:Rok.Commons"
      xmlns:artists1="using:Rok.ViewModels.Artists"
      x:Name="artistsView"
      mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource x:Name="groupedItemsViewSource" IsSourceGrouped="True" ItemsPath="Items" />

        <DataTemplate x:Key="ArtistGridTemplate" x:DataType="artists:ArtistViewModel">
            <Grid Style="{StaticResource MainGridCoverStyle}" PointerEntered="gridBottom_PointerEntered" PointerExited="gridBottom_PointerExited" Background="Transparent">
                <Grid.Resources>
                    <Storyboard x:Key="ShowGenreNameStoryboard">
                        <DoubleAnimation Storyboard.TargetName="genreName" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.6" />
                        <DoubleAnimation Storyboard.TargetName="subTitle" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5" />
                    </Storyboard>
                    <Storyboard x:Key="ShowSubTitleStoryboard">
                        <DoubleAnimation Storyboard.TargetName="genreName" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.6" />
                        <DoubleAnimation Storyboard.TargetName="subTitle" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                    </Storyboard>

                    <Storyboard x:Key="ShowFavoriteButtonStoryboard">
                        <DoubleAnimation Storyboard.TargetName="favoriteButton" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.6" />
                    </Storyboard>
                    <Storyboard x:Key="HideFavoriteButtonStoryboard">
                        <DoubleAnimation Storyboard.TargetName="favoriteButton" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                    </Storyboard>
                </Grid.Resources>

                <Border CornerRadius="{StaticResource CornerRadiusMediumValue}">
                    <common:PictureControl Style="{StaticResource PictureCoverStyle}" Cover="{x:Bind Picture, Mode=OneWay}" x:Phase="2" PlayButtonVisibility="Visible" Command="{x:Bind ListenCommand}" />
                </Border>

                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Orientation="Vertical" Spacing="{StaticResource SpacingXSmall}" Margin="0,8,8,0">
                    <common:BadgeControl x:Uid="badgeNew" Visibility="{x:Bind ShowNewBadge, Converter={StaticResource BoolToVisibilityConverter}}" BadgeBackground="{StaticResource BadgeNewBrush}" />
                </StackPanel>

                <Grid x:Name="gridBottom" Style="{StaticResource GridCoverBottomStyle}" Background="{x:Bind DominantColor, Converter={StaticResource ColorToGradientBrushConverter}, Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="{StaticResource GridBottomFirstRowHeight}" />
                        <RowDefinition Height="{StaticResource GridBottomSecondRowHeight}" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="{StaticResource FavoriteButtonColumnWidth}" />
                    </Grid.ColumnDefinitions>

                    <HyperlinkButton Content="{x:Bind Artist.Name}" x:Phase="0" Command="{x:Bind ArtistOpenCommand}" Style="{StaticResource GridCoverButtonStyle}" />
                    
                    <TextBlock x:Name="subTitle" Grid.Row="1" Style="{StaticResource GridCoverBottomSecondLineStyle}" Margin="4,6,0,0" Text="{x:Bind SubTitle, Mode=OneWay}" Opacity="1" x:Phase="1" />
                    <HyperlinkButton x:Name="genreName" Grid.Row="1" Style="{StaticResource GridCoverButtonStyle}" Content="{x:Bind Artist.GenreName, Mode=OneWay}" Command="{x:Bind GenreOpenCommand}" Opacity="0" x:Phase="1" />

                    <common:AnimatedButton x:Name="favoriteButton"
                                           Grid.Row="0" Grid.Column="1"                                           
                                           HorizontalAlignment="Right" VerticalAlignment="Stretch"
                                           Margin="0"
                                           Opacity="{x:Bind IsFavorite, Converter={StaticResource BoolToOpacityConverter}, Mode=OneWay}"
                                           Icon="{StaticResource HeartPath}"
                                           Color="{x:Bind IsFavorite, Converter={StaticResource FavoriteToColorConverter}, Mode=OneWay}"
                                           Command="{x:Bind ArtistFavoriteCommand}"
                                           CommandParameter="{x:Bind IsFavorite, Converter={StaticResource BoolToInvertBoolConverter}, Mode=OneWay}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ArtistListTemplate" x:DataType="artists:ArtistViewModel">
            <Grid Padding="12,4" HorizontalAlignment="Stretch" Background="Transparent" PointerEntered="gridBottom_PointerEntered" PointerExited="gridBottom_PointerExited">
                <Grid.Resources>
                    <Storyboard x:Key="ShowGenreNameStoryboard">
                        <DoubleAnimation Storyboard.TargetName="genreName" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.6" />
                        <DoubleAnimation Storyboard.TargetName="subTitle" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5" />
                    </Storyboard>
                    <Storyboard x:Key="ShowSubTitleStoryboard">
                        <DoubleAnimation Storyboard.TargetName="genreName" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.6" />
                        <DoubleAnimation Storyboard.TargetName="subTitle" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2" />
                    </Storyboard>

                    <Storyboard x:Key="ShowFavoriteButtonStoryboard">
                        <DoubleAnimation Storyboard.TargetName="favoriteButton" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.6" />
                    </Storyboard>
                    <Storyboard x:Key="HideFavoriteButtonStoryboard">
                        <DoubleAnimation Storyboard.TargetName="favoriteButton" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                    </Storyboard>
                </Grid.Resources>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{StaticResource ListCoverColumnWidth}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border CornerRadius="{StaticResource ControlCornerRadius}" Width="{StaticResource ListCoverWidth}" Height="{StaticResource ListCoverHeight}" VerticalAlignment="Center">
                    <common:PictureControl Cover="{x:Bind Picture, Mode=OneWay}" PlayButtonVisibility="Visible" Command="{x:Bind ListenCommand}" />
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource SpacingSmall}">
                        <HyperlinkButton Content="{x:Bind Artist.Name}" Command="{x:Bind ArtistOpenCommand}" Style="{StaticResource HyperlinkSmallButtonStyle}" Margin="4,0,0,0" FontSize="{StaticResource FontSizeXMedium}" FontWeight="SemiBold" />
                        <common:AnimatedButton x:Name="favoriteButton" HorizontalAlignment="Right" VerticalAlignment="Center" Icon="{StaticResource HeartPath}" Color="{x:Bind IsFavorite, Converter={StaticResource FavoriteToColorConverter}, Mode=OneWay}" Opacity="0" Command="{x:Bind ArtistFavoriteCommand}" CommandParameter="{x:Bind IsFavorite, Converter={StaticResource BoolToInvertBoolConverter}, Mode=OneWay}" />
                    </StackPanel>

                    <Grid Height="20">
                        <TextBlock x:Name="subTitle" Text="{x:Bind SubTitle, Mode=OneWay}" Style="{StaticResource ListCoverBottomSecondLineStyle}" VerticalAlignment="Center" />
                        <HyperlinkButton x:Name="genreName" Content="{x:Bind Artist.GenreName, Mode=OneWay}" Command="{x:Bind GenreOpenCommand}" Style="{StaticResource HyperlinkSmallButtonStyle}" Margin="4,0,0,0" FontSize="{StaticResource FontSizeSmall}" Opacity="0" VerticalAlignment="Center" Padding="0" />
                    </Grid>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="DisplayModes">
                <VisualState x:Name="GridViewState" />
                <VisualState x:Name="ListViewState">
                    <VisualState.Setters>
                        <Setter Target="grid.ItemTemplate" Value="{StaticResource ArtistListTemplate}" />
                        <Setter Target="grid.ItemsPanel" Value="{StaticResource VerticalListPanelTemplate}" />
                        <Setter Target="grid.ItemContainerStyle" Value="{StaticResource ListModeGridViewItemStyle}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource HeaderListRowHeight}" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" Style="{StaticResource HeaderCommandBarListStyle}">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="{StaticResource SpacingSmall}">

                    <AppBarButton x:Uid="artistViewListen" Style="{StaticResource ListenAppBarButtonCompactStyle}" Command="{x:Bind ViewModel.ListenCommand}" />

                    <SplitButton x:Name="groupBySplitButton" VerticalAlignment="Center">
                        <SplitButton.Content>
                            <StackPanel Style="{StaticResource CommandBarSplitButtonPanelStyle}" AutomationProperties.Name="{x:Bind ViewModel.GroupByText, Mode=OneWay}" ToolTipService.ToolTip="{x:Bind ViewModel.GroupByText, Mode=OneWay}">
                                <FontIcon Glyph="&#xE8CB;" Style="{StaticResource CommandBarSplitButtonIconStyle}" />
                                <TextBlock x:Uid="artistsViewGroupBy" Style="{StaticResource CommandBarSplitButtonLabelTextStyle}" />
                                <TextBlock Text="{x:Bind ViewModel.GroupByText, Mode=OneWay}" Style="{StaticResource CommandBarSplitButtonValueTextStyle}" />
                            </StackPanel>
                        </SplitButton.Content>
                        <SplitButton.Flyout>
                            <MenuFlyout x:Name="groupByMenu" ShowMode="TransientWithDismissOnPointerMoveAway" Placement="RightEdgeAlignedBottom" Opened="GroupByFlyout_Opened" />
                        </SplitButton.Flyout>
                    </SplitButton>

                    <SplitButton x:Name="buttonFilterSplitButton" VerticalAlignment="Center">
                        <SplitButton.Content>
                            <StackPanel Style="{StaticResource CommandBarSplitButtonPanelStyle}" AutomationProperties.Name="{x:Bind ViewModel.FilterByText, Mode=OneWay}" ToolTipService.ToolTip="{x:Bind ViewModel.FilterByText, Mode=OneWay}">
                                <FontIcon Glyph="&#xE71C;" Style="{StaticResource CommandBarSplitButtonIconStyle}" />
                                <TextBlock x:Uid="artistsViewFilterBy" Style="{StaticResource CommandBarSplitButtonLabelTextStyle}" />
                                <TextBlock Text="{x:Bind ViewModel.FilterByText, Mode=OneWay}" Style="{StaticResource CommandBarSplitButtonValueTextStyle}" />
                            </StackPanel>
                        </SplitButton.Content>
                        <SplitButton.Flyout>
                            <MenuFlyout x:Name="filterMenu" ShowMode="TransientWithDismissOnPointerMoveAway" Placement="RightEdgeAlignedBottom" Opened="FilterFlyout_Opened" />
                        </SplitButton.Flyout>
                    </SplitButton>

                    <AutoSuggestBox x:Uid="inlineListSearch" QueryIcon="Find" Style="{StaticResource InlineSearchStyle}" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <AppBarSeparator />
                    <AppBarButton x:Name="gridListModeButton" Style="{StaticResource AppBarButtonCompactStyle}" Icon="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource GridListIconConverter}}" Command="{x:Bind ViewModel.ToggleDisplayModeCommand}" />
                    <AppBarSeparator />

                    <!-- Info about albums count and duration -->
                    <StackPanel Style="{StaticResource CommandBarStatInfoPanelStyle}" Margin="12,0,0,0">
                        <FontIcon Glyph="&#xE93C;" Style="{StaticResource CommandBarStatInfoIconStyle}" />
                        <TextBlock Style="{StaticResource CommandBarStatInfoTextStyle}">
                             <Run x:Name="artistCountRun" FontWeight="SemiBold" />
                             <Run x:Uid="artistsPageArtists" />
                        </TextBlock>
                        <TextBlock Style="{StaticResource CommandBarStatInfoSeparatorStyle}" />
                        <FontIcon Glyph="&#xE916;" Style="{StaticResource CommandBarStatInfoIconStyle}" />
                        <TextBlock Style="{StaticResource CommandBarStatInfoTextStyle}">
                            <Run x:Name="artistDurationRun"
                                 FontWeight="SemiBold" />
                             <Run x:Uid="hours" />
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>

        <Grid Grid.Row="1" Style="{StaticResource GridListStyle}">
            <SemanticZoom x:Name="GridZoom" ScrollViewer.ZoomMode="Enabled" IsZoomOutButtonEnabled="True">
                <SemanticZoom.ZoomedInView>
                    <common:GridViewExtended x:Name="grid"
                                             ContainerContentChanging="GridContainerContentChanging"
                                             BindableSelectedItems="{x:Bind ViewModel.Selected, Mode=OneWay}"
                                             ItemTemplate="{StaticResource ArtistGridTemplate}"
                                             ItemsPanel="{StaticResource GridPanelTemplate}"
                                             SelectionMode="Multiple"
                                             IsMultiSelectCheckBoxEnabled="{x:Bind ViewModel.IsSelectedItems, Mode=OneWay}"
                                             IsItemClickEnabled="True"
                                             Margin="0"
                                             ScrollViewer.VerticalScrollMode="Enabled"
                                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <GridView.GroupStyle>
                            <GroupStyle HidesIfEmpty="True">
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate x:DataType="artists1:ArtistsGroupCategoryViewModel">
                                        <StackPanel Orientation="Horizontal">
                                            <HyperlinkButton Content="{x:Bind Title}" Style="{StaticResource HyperlinkGroupButtonStyle}" />

                                            <Border Style="{StaticResource GroupHeaderCountBorderStyle}">
                                                <TextBlock Text="{x:Bind Count}" Style="{StaticResource GroupHeaderCountTextStyle}" />
                                            </Border>

                                            <Button Style="{StaticResource GroupHeaderListenButtonStyle}" Command="{Binding ElementName=artistsView, Path=DataContext.ListenGroupCommand}" CommandParameter="{x:Bind}" Tapped="GroupButton_Tapped">
                                                <StackPanel Orientation="Horizontal">
                                                    <FontIcon Glyph="&#xEE4A;" Margin="0,0,6,0" FontSize="{ThemeResource ControlContentThemeFontSize}" VerticalAlignment="Center" />
                                                    <TextBlock x:Uid="artistsViewListenListGroup" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>

                                <GroupStyle.Panel>
                                    <ItemsPanelTemplate>
                                        <VariableSizedWrapGrid Orientation="Vertical" />
                                    </ItemsPanelTemplate>
                                </GroupStyle.Panel>
                            </GroupStyle>
                        </GridView.GroupStyle>
                    </common:GridViewExtended>
                </SemanticZoom.ZoomedInView>

                <SemanticZoom.ZoomedOutView>
                    <GridView x:Name="ZoomoutCollectionGrid" VerticalAlignment="Center" ItemTemplate="{StaticResource SemanticZoomOutGroupItemDataTemplate}" ItemsPanel="{StaticResource SemanticZoomOutGroupPanelTemplate}" />
                </SemanticZoom.ZoomedOutView>
            </SemanticZoom>

            <common:EmptyStateControl x:Uid="artistsViewNoData" Glyph="&#xE93C;" Visibility="{x:Bind ViewModel.HasNoData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </Grid>
</Page>