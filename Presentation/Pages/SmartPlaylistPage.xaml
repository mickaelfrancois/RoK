<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Rok.Pages.SmartPlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Rok.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:common="using:Rok.Commons" 
    xmlns:track="using:Rok.ViewModels.Track"
    x:Name="smartPlaylistPage"    
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="headerRow" Height="{StaticResource HeaderRowHeight}" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.Background>
            <SolidColorBrush Color="{ThemeResource CardBackgroundFillColorDefault}" Opacity="{StaticResource HeaderBackgroundOpacity}" />
        </Grid.Background>

        <Image x:Name="backdrop" Grid.RowSpan="2" Style="{StaticResource HeaderBackdropStyle}" Source="{x:Bind ViewModel.Backdrop,Mode=OneWay}" Stretch="UniformToFill" IsHitTestVisible="False" />
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="pictureColumn" Width="{StaticResource HeaderPictureColumnWidth}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Style="{StaticResource HeaderPictureGridStyle}">
                <common:PictureControl x:Name="artistPicture" Style="{StaticResource HeaderPictureStyle}" Cover="{x:Bind ViewModel.Picture, Mode=OneWay}" />
            </Grid>

            <RelativePanel Grid.Column="1" Margin="6,0,0,0">
                <common:InlineEditableText x:Name="playlistName" Text="{x:Bind ViewModel.Name, Mode=TwoWay}" TextStyle="{StaticResource HeaderStyle}" />
                <TextBlock x:Name="panelExtendedInfo" RelativePanel.Below="playlistName" Style="{StaticResource HeaderTextStyle}" Text="{x:Bind ViewModel.SubTitle, Mode=OneWay}" />

                <CommandBar RelativePanel.AlignBottomWithPanel="True" Style="{StaticResource HeaderCommandBarStyle}">
                    <AppBarButton x:Uid="playlistListen" Style="{StaticResource ListenAppBarButtonStyle}" Command="{x:Bind ViewModel.ListenCommand}" />
                    <AppBarButton x:Uid="playlistRefresh" Style="{StaticResource AppBarButtonCompactStyle}" Icon="Refresh" Command="{x:Bind ViewModel.GenerateCommand}" />
                    <AppBarButton x:Uid="playlistDelete" Style="{StaticResource AppBarButtonCompactStyle}" Icon="Delete" Click="DeleteButton_Click" />
                </CommandBar>
            </RelativePanel>
        </Grid>


        <Pivot x:Name="playlistPivot" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <PivotItem x:Uid="playlistViewTabConfiguration">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"></RowDefinition>
                        <RowDefinition Height="40"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Uid="PlaylistTrackCount" VerticalAlignment="Center" Margin="0,0,8,0" Text="Nombre de titres :"></TextBlock>
                        <NumberBox VerticalAlignment="Center" Text="{x:Bind ViewModel.TrackMaximum, Mode=TwoWay}" Width="50" HorizontalAlignment="Left"></NumberBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Grid.Row="2" Click="PlaylistSave_Click" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Save"></SymbolIcon>
                                <TextBlock x:Uid="playlistSave" FontFamily="Segoe UI" Margin="2,0,0,0" VerticalAlignment="Center"></TextBlock>
                            </StackPanel>
                        </Button>
                        <Button Click="GroupNew_Click">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Add"></SymbolIcon>
                                <TextBlock x:Uid="playlistGroupNew" Text="Nouveau groupe" FontFamily="Segoe UI" Margin="2,0,0,0" VerticalAlignment="Center"></TextBlock>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <ListBox x:Name="lvGroup" Grid.Row="2" Background="Transparent" BorderThickness="0,1,0,0" BorderBrush="Gray" />
                </Grid>
            </PivotItem>

            <PivotItem x:Uid="playlistViewTabTracks" x:Name="playlistViewTabTracks">
                <Grid Style="{StaticResource FileGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="{StaticResource GridHeaderTracksHeight}" />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <!-- Grid headers -->
                    <Grid Style="{StaticResource TracksGridStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{StaticResource GridHeaderTracksTitleColumnWidth}" />
                            <ColumnDefinition Width="{StaticResource GridHeaderTracksArtistColumnWidth}" />
                            <ColumnDefinition Width="{StaticResource GridHeaderTracksAlbumColumnWidth}" />
                            <ColumnDefinition Width="{StaticResource GridHeaderTracksScoreColumnWidth}" />
                        </Grid.ColumnDefinitions>

                        <HyperlinkButton Grid.Column="0" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Title" Style="{StaticResource HeaderHyperlinkStyle}" Margin="20,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Uid="gridHeaderTitle" Style="{StaticResource GridHeaderTracksTextBlockStyle}" Margin="0" />
                                <FontIcon Glyph="{x:Bind ViewModel.GetSortGlyph(ViewModel.SortDescending), Mode=OneWay}" FontSize="{StaticResource FontSizeXSmall}" Margin="5,0,0,0" Visibility="{x:Bind ViewModel.IsTitleSorted, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </StackPanel>
                        </HyperlinkButton>

                        <HyperlinkButton Grid.Column="1" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Artist" Style="{StaticResource HeaderHyperlinkStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Uid="gridHeaderArtist" Style="{StaticResource GridHeaderTracksTextBlockStyle}" Margin="0" />
                                <FontIcon Glyph="{x:Bind ViewModel.GetSortGlyph(ViewModel.SortDescending), Mode=OneWay}" FontSize="{StaticResource FontSizeXSmall}" Margin="5,0,0,0" Visibility="{x:Bind ViewModel.IsArtistSorted, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </StackPanel>
                        </HyperlinkButton>

                        <HyperlinkButton Grid.Column="2" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Album" Style="{StaticResource HeaderHyperlinkStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Uid="gridHeaderAlbum" Style="{StaticResource GridHeaderTracksTextBlockStyle}" Margin="0" />
                                <FontIcon Glyph="{x:Bind ViewModel.GetSortGlyph(ViewModel.SortDescending), Mode=OneWay}" FontSize="{StaticResource FontSizeXSmall}" Margin="5,0,0,0" Visibility="{x:Bind ViewModel.IsAlbumSorted, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </StackPanel>
                        </HyperlinkButton>

                        <HyperlinkButton Grid.Column="3" Command="{x:Bind ViewModel.SortCommand}" CommandParameter="Score" Style="{StaticResource HeaderHyperlinkStyle}" Margin="20,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Uid="gridHeaderScore" Style="{StaticResource GridHeaderTracksTextBlockStyle}" Margin="0" />
                                <FontIcon Glyph="{x:Bind ViewModel.GetSortGlyph(ViewModel.SortDescending), Mode=OneWay}" FontSize="{StaticResource FontSizeXSmall}" Margin="5,0,0,0" Visibility="{x:Bind ViewModel.IsScoreSorted, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                            </StackPanel>
                        </HyperlinkButton>
                    </Grid>

                    <common:ListViewExtended x:Name="tracksList" ItemsSource="{x:Bind ViewModel.Tracks}" ContainerContentChanging="TracksListContainerContentChanging" Grid.Row="1" BorderThickness="0" ItemContainerStyle="{StaticResource TracksListViewItemStyle}" Margin="0" IsItemClickEnabled="True">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="track:TrackViewModel">
                                <Grid Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="{StaticResource GridHeaderTracksTitleColumnWidth}" />
                                        <ColumnDefinition Width="{StaticResource GridHeaderTracksArtistColumnWidth}" />
                                        <ColumnDefinition Width="{StaticResource GridHeaderTracksAlbumColumnWidth}" />
                                        <ColumnDefinition Width="{StaticResource GridHeaderTracksScoreColumnWidth}" />
                                    </Grid.ColumnDefinitions>

                                    <Grid.ContextFlyout>
                                        <MenuFlyout ShowMode="TransientWithDismissOnPointerMoveAway">
                                            <MenuFlyoutItem x:Uid="trackFlyoutRemoveFromPlaylist" Command="{Binding ViewModel.RemoveFromPlaylistCommand, ElementName=playlistPage}" CommandParameter="{x:Bind Track.Id}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xECC9;" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutSeparator />
                                            <MenuFlyoutItem x:Uid="trackFlyoutArtist" Command="{x:Bind ArtistOpenCommand}">
                                                <MenuFlyoutItem.Icon>
                                                    <IconSourceElement IconSource="{StaticResource IconPeople}" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem x:Uid="trackFlyoutAlbum" Command="{x:Bind AlbumOpenCommand}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE93C;" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem x:Uid="trackFlyoutTrack" Command="{x:Bind TrackOpenCommand}">
                                                <MenuFlyoutItem.Icon>
                                                    <IconSourceElement IconSource="{StaticResource IconAudio}" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutSeparator />
                                            <MenuFlyoutItem x:Uid="trackFlyoutLyrics" Command="{x:Bind LyricsOpenCommand}" Visibility="{x:Bind LyricsExists, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE90B;" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Grid.ContextFlyout>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock x:Name="RowIndexText" Style="{StaticResource GridTracksRowIndexTextBlockStyle}" />
                                        <HyperlinkButton Style="{StaticResource GridTracksTitleHyperlinkButtonStyle}" Command="{Binding DataContext.ListenCommand, ElementName=smartPlaylistPage}" CommandParameter="{Binding}" />
                                        <HyperlinkButton x:Uid="lyricsTag" Visibility="{x:Bind LyricsExists, Converter={StaticResource BoolToVisibilityConverter}}" Style="{StaticResource GridTracksLyricsHyperlinkButtonStyle}" Command="{x:Bind LyricsOpenCommand}" />
                                    </StackPanel>

                                    <HyperlinkButton Grid.Column="1" Style="{StaticResource GridTracksArtistHyperlinkButtonStyle}" Command="{x:Bind ArtistOpenCommand}" />
                                    <HyperlinkButton Grid.Column="2" Style="{StaticResource GridTracksAlbumHyperlinkButtonStyle}" Command="{x:Bind AlbumOpenCommand}" />

                                    <common:RatingControlLightControl Grid.Column="3" Style="{StaticResource GridTracksScoreStyle}" RatingValue="{x:Bind Score, Mode=TwoWay}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </common:ListViewExtended>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
