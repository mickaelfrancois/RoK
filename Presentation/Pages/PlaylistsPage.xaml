<Page x:Class="Rok.Pages.PlaylistsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Rok.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:common="using:Rok.Commons"
      xmlns:a="using:Rok.ViewModels.Playlist"
      mc:Ignorable="d"
      x:Name="playlistsView">

    <Page.Resources>
        <DataTemplate x:Key="PlaylistGridTemplate" x:DataType="a:PlaylistViewModel">
            <Grid Style="{StaticResource MainGridCoverStyle}">
                <Border CornerRadius="{StaticResource CornerRadiusMediumValue}">
                    <common:PictureControl Style="{StaticResource PictureCoverStyle}" Cover="{x:Bind Picture, Mode=OneWay}" x:Phase="2" PlayButtonVisibility="Visible" Command="{x:Bind ListenCommand}" />
                </Border>

                <Grid x:Name="gridBottom" Style="{StaticResource GridCoverBottomStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="{StaticResource GridBottomFirstRowHeight}" />
                        <RowDefinition Height="{StaticResource GridBottomSecondRowHeight}" />
                    </Grid.RowDefinitions>

                    <HyperlinkButton Content="{x:Bind Playlist.Name}" Command="{x:Bind PlaylistOpenCommand}" Style="{StaticResource GridCoverButtonStyle}" />
                    <TextBlock Grid.Row="1" Style="{StaticResource GridCoverBottomSecondLineStyle}" Text="{x:Bind SubTitle, Mode=OneWay}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="PlaylistListTemplate" x:DataType="a:PlaylistViewModel">
            <Grid Padding="12,4" HorizontalAlignment="Stretch" Background="Transparent">

                <Border CornerRadius="{StaticResource CornerRadiusMediumValue}" Width="{StaticResource ListCoverWidth}" Height="{StaticResource ListCoverHeight}" VerticalAlignment="Center">
                    <common:PictureControl Cover="{x:Bind Picture, Mode=OneWay}" PlayButtonVisibility="Visible" Command="{x:Bind ListenCommand}" />
                </Border>

                <StackPanel VerticalAlignment="Center" Margin="12,0,0,0">
                    <HyperlinkButton Content="{x:Bind Playlist.Name}" Command="{x:Bind PlaylistOpenCommand}" Style="{StaticResource HyperlinkSmallButtonStyle}" Margin="4,0,0,0" FontSize="{StaticResource FontSizeXMedium}" FontWeight="SemiBold" />

                    <Grid Height="20">
                        <TextBlock x:Name="subTitle" Text="{x:Bind SubTitle, Mode=OneWay}" Style="{StaticResource ListCoverBottomSecondLineStyle}" VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right" Style="{StaticResource HeaderCommandBarListStyle}">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="{StaticResource SpacingSmall}">
                    <AppBarButton x:Uid="playlistsAdd"
                                  Style="{StaticResource AppBarButtonCompactStyle}" Icon="Add" Command="{x:Bind ViewModel.NewPlaylistCommand}" />
                    <AppBarButton x:Uid="playlistsSmartAdd" Style="{StaticResource AppBarButtonCompactStyle}" Icon="OutlineStar" Command="{x:Bind ViewModel.NewSmartPlaylistCommand}" />
                    <AppBarSeparator />
                    <AppBarButton Icon="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource GridListIconConverter}}" Style="{StaticResource AppBarButtonCompactStyle}" Command="{x:Bind ViewModel.ToggleDisplayModeCommand}" />
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>

        <Grid Grid.Row="1" Style="{StaticResource GridListStyle}">
            <ScrollViewer>
                <StackPanel>
                    <GridViewHeaderItem FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold" HorizontalAlignment="Left">
                        <GridViewHeaderItem.Content>
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="OutlineStar" Margin="4,0,4,0" />
                                <TextBlock x:Uid="smartPlaylistHeader" />
                            </StackPanel>
                        </GridViewHeaderItem.Content>
                    </GridViewHeaderItem>

                    <common:GridViewExtended x:Name="gridSmartPlaylist"
                                             ContainerContentChanging="grid_ContainerContentChanging"
                                             ItemsSource="{x:Bind ViewModel.SmartPlaylists}"
                                             ItemTemplate="{StaticResource PlaylistGridTemplate}"
                                             ItemsPanel="{StaticResource GridPanelTemplate}"
                                             Visibility="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                             SelectionMode="None"
                                             IsItemClickEnabled="True"
                                             ScrollViewer.VerticalScrollMode="Disabled" />

                    <ListView x:Name="listSmartPlaylist"
                              ContainerContentChanging="grid_ContainerContentChanging"
                              ItemsSource="{x:Bind ViewModel.SmartPlaylists}"
                              ItemTemplate="{StaticResource PlaylistListTemplate}"
                              Visibility="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource BoolToInvertVisibilityConverter}}"
                              SelectionMode="None"
                              IsItemClickEnabled="True" />

                    <GridViewHeaderItem FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold" HorizontalAlignment="Left">
                        <GridViewHeaderItem.Content>
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Shop" Margin="4,0,4,0" />
                                <TextBlock x:Uid="playlistHeader" />
                            </StackPanel>
                        </GridViewHeaderItem.Content>
                    </GridViewHeaderItem>

                    <common:GridViewExtended x:Name="gridPlaylist"
                                             ContainerContentChanging="grid_ContainerContentChanging"
                                             ItemsSource="{x:Bind ViewModel.Playlists}"
                                             ItemTemplate="{StaticResource PlaylistGridTemplate}"
                                             ItemsPanel="{StaticResource GridPanelTemplate}"
                                             Visibility="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                             SelectionMode="None"
                                             IsItemClickEnabled="True"
                                             ScrollViewer.VerticalScrollMode="Disabled" />

                    <ListView x:Name="listPlaylist"
                              ContainerContentChanging="grid_ContainerContentChanging"
                              ItemsSource="{x:Bind ViewModel.Playlists}"
                              ItemTemplate="{StaticResource PlaylistListTemplate}"
                              Visibility="{x:Bind ViewModel.IsGridView, Mode=OneWay, Converter={StaticResource BoolToInvertVisibilityConverter}}"
                              SelectionMode="None"
                              IsItemClickEnabled="True" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
