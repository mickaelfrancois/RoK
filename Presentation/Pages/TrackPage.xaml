<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Rok.Pages.TrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Rok.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:winui="using:Microsoft.UI.Xaml.Controls"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="headerRow"
                           Height="110" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.Background>
            <SolidColorBrush Color="{ThemeResource CardBackgroundFillColorDefault}"
                             Opacity="{StaticResource headerBackgroundOpacity}" />
        </Grid.Background>

        <Image x:Name="backdrop"
               Grid.RowSpan="2"
               Source="{x:Bind ViewModel.Backdrop,Mode=OneWay}"
               Opacity="0.20"
               Style="{StaticResource headerBackdrop}"
               Stretch="UniformToFill"
               IsHitTestVisible="False" />

        <StackPanel Grid.Row="0" Orientation="Vertical" Padding="6" Spacing="4">
            <TextBlock Text="{x:Bind ViewModel.Track.Title}"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="{ThemeResource SystemAccentColor }" />

            <StackPanel Orientation="Horizontal">
                <TextBlock x:Uid="genByLabel" 
                           FontSize="20"
                           Margin="0,0,6,0" />
                <TextBlock Text="{x:Bind ViewModel.ArtistName}"
                           FontSize="20"
                           FontWeight="SemiBold" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock x:Uid="genOnLabel" 
                           Margin="0,0,6,0" />
                <TextBlock Text="{x:Bind ViewModel.AlbumName}"
                           FontStyle="Italic" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorDefault}"
                Padding="24"
                CornerRadius="12"
                Margin="8">
            <StackPanel>

                <StackPanel Orientation="Horizontal"
                            Spacing="24">
                    <StackPanel Orientation="Horizontal"
                                Spacing="8">
                        <SymbolIcon Symbol="Audio"
                                    Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="{x:Bind ViewModel.BitrateStr}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Spacing="8">
                        <SymbolIcon Symbol="Save"
                                    Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="{x:Bind ViewModel.SizeStr}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Spacing="8">
                        <FontIcon Glyph="&#xE823;"
                                  Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="{x:Bind ViewModel.DurationStr}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Spacing="8"
                                Visibility="{x:Bind ViewModel.Score, Converter={StaticResource IntToVisibilityConverter}}">
                        <SymbolIcon Symbol="SolidStar"
                                    Foreground="{ThemeResource SystemAccentColor}" />
                        <TextBlock Text="{x:Bind ViewModel.Score}" />
                    </StackPanel>

                </StackPanel>

                <StackPanel Margin="0,16,0,0">

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackListenCountLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.ListenCount, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackLastListenLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.LastListen, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackSkipCountLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.SkipCount, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackLastSkipLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.LastSkip, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackMusicBrainzIdLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.MusicBrainzID, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock x:Uid="trackStorageLabel"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="{x:Bind ViewModel.Track.MusicFile, Mode=OneWay}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
