<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Rok.Commons.EmptyStateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="{StaticResource SpacingMedium}">
        <FontIcon Glyph="{x:Bind Glyph, Mode=OneWay}" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                  FontSize="{StaticResource FontSizeXLarge}" Opacity="0.4" />
        <TextBlock Text="{x:Bind Message, Mode=OneWay}" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}" Opacity="0.6" />
    </StackPanel>
</UserControl>